name: Event Workflow Demo

on:
    pull_request:
        branches:
            - main
        types:
            - opened
    workflow_dispatch:

jobs:
    lint-test-build-deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Get code
              uses: actions/checkout@v3
            - name: Set up Node.js
              uses: actions/setup-node@v3
              with:
                  node-version: "18"
            - name: Install dependencies
              run: npm ci
            - name: Run ESLint
              run: npm run lint
            - name: Run tests
              run: npm test
            - name: Build project
              run: npm run build
            - name: Deploy to Server
              run: echo "Deploying to server..."
